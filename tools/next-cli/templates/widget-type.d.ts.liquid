// This file is generated by @ossinsight/cli

declare module '@ossinsight/internal/widgets' {
  interface WidgetsDefinitions {
    {%- for package in manifest %}
    /**
     * {{ package.description }}
     */
    '{{ package.name }}': {
      isPrivate: {% if package.private %}true{% else %}false{% endif %},
      params: {
        {%- for param in package.params %}
        {{ param[0] }}
        {%- if param[1].required != true %}?{% endif %}: {{ typeMap[param[1].type] | ifnull: 'any' }}
        {%- if param[1].title %} // {{ param[1].title }}{%- endif -%}
        {%- endfor %}
      }
    }
    {% endfor %}
  }
}
