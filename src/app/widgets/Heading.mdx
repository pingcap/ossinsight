import ArrowUpRightIcon from 'bootstrap-icons/icons/arrow-up-right.svg'
import links from './outlinks';

<div className='grid grid-cols-12 gap-4'>
  <section className='col-span-12 sm:col-span-6 md:col-span-7 lg:col-span-9'>
    # Customize GitHub Widgets - Data Insight, Social Sharing!

    Explore our practical widgets to analyze GitHub data, **share on social media**, **embed in repositories or personal README file**, and **create engaging presentations**! See demos:

    <div className='mt-4 not-prose'>
      <ul className='flex gap-8 items-center'>
        {links.map(link => (
          <li key={link.title}>
            <a className='flex items-center gap-2 cursor-pointer' href={link.href} target={/^https?:/.test(link.href) ? '_blank' : '_self'}>
              {link.icon}
              {link.title}
              {<ArrowUpRightIcon width='14' height='14' />}
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section className='col-span-12 sm:col-span-6 md:col-span-5 lg:col-span-3 flex items-center justify-end'>
    <iframe
      className='w-full sm:max-w-xs aspect-video bg-toolbar rounded'
      src="https://www.youtube.com/embed/An0VW0k_CKA?autoplay=1&loop=1&rel=0&playlist=An0VW0k_CKA&showinfo=0&mute=1"
      title="YouTube video player" frameBorder="0"
      allow="accelerometer; autoplay; muted; loop; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
      allowFullScreen
    />
  </section>
</div>
